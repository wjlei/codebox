#include<iostream>
#include<stdio.h>
#include<math.h>
#define PI acos(-1)
using namespace std;
int main(){
  int T;
  cin>>T;
  while(T--){
    int x;
    scanf("%d",&x);
    printf("%d\n",(int)ceil((0.5*log(2*x*PI)+x*log(x)-x)/log(10)));
  }
  return 1;
}
